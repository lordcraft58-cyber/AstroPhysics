# Dependencias mínimas para ejecutar tests/ contra legacy/AstroPhysicsSuite_v57_3_COMMERCIAL.py.
# Esto es un punto de partida para la Fase 3-5 (tests de regresión), NO el
# manifiesto de dependencias fijado del producto -- ver requirements-app.txt
# (auditado y acotado para el build comercial de Windows, Fase 9 -- ver
# packaging/README.md), que además omite pytest (solo se necesita en
# desarrollo, nunca en el producto empaquetado).
numpy>=2.0
scipy>=1.13
astropy>=6.0
photutils>=1.13
matplotlib>=3.8
openpyxl>=3.1
pytest>=8.0
astroquery>=0.4  # Gaia/SIMBAD -- ver astrophysics_suite/catalogs/
